# 「先存着」AI 知识卡片工具

## Codex 需求文档 v0.4

> 本文用于指导 Codex 在现有项目框架基础上完成产品方向调整，也可作为产品、设计和开发团队的统一需求说明。  
> 当前版本将产品从“自动解析多平台内容的智能收藏夹”收窄为“手动输入内容、AI 生成知识卡片、支持低压力回顾的个人知识工具”。

---

## 1. 产品定位

### 1.1 产品名称

暂定名称：**先存着**

### 1.2 一句话定位

> 把真正想记住的内容输入进来，AI 帮你整理成知识卡片，并在合适的时候帮助你回顾。

### 1.3 产品定义

「先存着」是一款面向个人用户的 AI 知识卡片工具。

用户可以手动输入或粘贴一段内容，并补充标题、来源和保存原因。系统对内容进行摘要、分类、标签提取和知识卡片生成。用户确认后，卡片进入个人知识库，并可以通过轻量、低压力的方式进行回顾。

产品不再依赖对小红书、抖音、视频和图片链接的自动解析来完成核心流程。

### 1.4 核心产品闭环

```text
用户输入或粘贴内容
        ↓
系统立即保存原文
        ↓
AI 生成摘要、标签和候选卡片
        ↓
用户确认、编辑或删除
        ↓
卡片进入个人知识库
        ↓
用户通过回顾重新记忆和使用
```

---

## 2. 本次方向调整

### 2.1 保留的能力

在现有项目中，以下能力如果已经存在，应优先复用：

- 用户登录；
- 用户数据隔离；
- 移动端页面框架；
- 收藏或内容列表；
- 内容详情页；
- AI 处理任务；
- 私密数据设置；
- 基础设置页；
- 数据删除能力；
- 基础 UI 组件和设计系统。

### 2.2 降级或移除的能力

以下内容不再属于 MVP 核心：

- 自动爬取小红书、抖音等平台；
- 自动解析视频画面；
- 自动识别视频中的文字；
- 自动解析图片内容；
- 自动获取所有平台的字幕；
- 从不完整链接内容中生成完整摘要；
- 非知识内容的复杂结构化处理；
- 商品、地点、影视等专用收藏页面；
- 自动生成完整知识图谱；
- 公开收藏和社交功能；
- 强制打卡和连续学习机制。

### 2.3 来源链接的新定位

来源链接仍然可以保留，但只作为内容的补充信息：

- 用户可以填写来源链接；
- 系统保存来源链接；
- 详情页可以打开原始链接；
- MVP 不要求系统成功访问或解析来源链接；
- 来源链接解析失败不能阻塞内容保存；
- AI 只能基于用户输入的正文生成卡片。

---

## 3. 产品目标

### 3.1 MVP 目标

验证以下问题：

1. 用户是否愿意主动输入自己真正想记住的内容。
2. AI 生成的卡片是否能减少用户制作卡片的成本。
3. 用户是否愿意编辑和确认 AI 生成的卡片。
4. 用户是否愿意在之后再次回顾这些卡片。
5. 用户是否认为卡片内容可靠、清晰、有记忆价值。

### 3.2 北极星指标

> **每周完成回顾的有效知识卡片数量**

“有效知识卡片”指：

- 用户主动确认过；
- 有明确问题或记忆目标；
- 至少完成过一次回顾；
- 未被删除或归档。

### 3.3 关键指标

#### 内容创建

- 首次输入成功率；
- 首次生成卡片成功率；
- 每周新增内容数量；
- 每周新增卡片数量。

#### AI 质量

- 候选卡片确认率；
- 候选卡片编辑率；
- 候选卡片删除率；
- 用户重新生成卡片的比例；
- AI 结果生成失败率。

#### 回顾使用

- 首次回顾完成率；
- 7 日内再次回顾率；
- 每位活跃用户每周回顾次数；
- 卡片暂停率；
- 卡片归档率。

---

## 4. 目标用户

MVP 主要面向：

- 想记住读书、课程和文章内容的人；
- 需要学习专业知识的人；
- 准备考试或面试的人；
- 学习语言的人；
- 想沉淀工作经验和方法的人；
- 经常保存内容，但之后容易忘记的人；
- 不想学习复杂卡片语法的人。

暂不针对：

- 需要完整课程管理的学生；
- 需要多人协作的团队；
- 需要专业题库和考试统计的培训机构；
- 只想管理商品、旅行和影视收藏的用户。

---

## 5. 产品原则

### 5.1 先保存，再处理

用户提交正文后，应立即创建内容记录。AI 处理失败不能导致用户输入丢失。

### 5.2 AI 提供建议，用户拥有最终控制权

AI 生成的摘要、标签和卡片都必须允许用户：

- 编辑；
- 删除；
- 重新生成；
- 忽略；
- 手动创建替代内容。

### 5.3 原文是唯一可靠依据

AI 只能基于用户提供的正文生成结果。

如果正文为空、过短或无法理解：

- 不生成卡片；
- 显示明确的失败或信息不足状态；
- 引导用户补充内容。

### 5.4 卡片不是最终答案

候选卡片必须经过用户确认后，才能进入正式知识库和回顾流程。

### 5.5 回顾低压力

回顾是帮助用户重新想起内容的工具，不是打卡任务系统。

MVP 不提供：

- 连续天数；
- 逾期惩罚；
- 排行榜；
- 学习失败提示；
- 强制每日目标。

### 5.6 默认私密

用户输入的内容、卡片、回顾记录、标签和来源默认仅本人可见。

---

## 6. MVP 范围

### 6.1 P0 必须实现

- 登录；
- 测试环境登录；
- 手动输入内容；
- 粘贴文本；
- 输入标题；
- 输入保存原因；
- 输入来源链接；
- 保存原文；
- AI 生成摘要；
- AI 生成候选知识卡片；
- 编辑候选卡片；
- 确认卡片；
- 删除卡片；
- 卡片列表；
- 卡片详情；
- 基础搜索；
- 基础标签；
- 轻量回顾；
- 内容和卡片删除；
- 加载、空状态、错误和重试状态；
- 当前用户数据隔离。

### 6.2 P1 可以实现

- 批量生成卡片；
- 卡片类型选择；
- AI 自动标签；
- 收藏原文与卡片关联；
- 来源链接展示；
- 基础回顾反馈；
- 最近输入内容；
- “很久没有回顾”的卡片列表；
- Markdown 文本支持；
- 内容导出。

### 6.3 P2 暂不实现

- 图片 OCR；
- 语音转文字；
- iPhone 快捷指令；
- iOS 分享扩展；
- 浏览器扩展；
- 自动解析短视频；
- 视频字幕获取；
- 图片理解；
- 多平台收藏同步；
- 智能间隔重复算法；
- 多端原生 App；
- 多人协作；
- 社交分享。

---

## 7. 核心用户流程

### 7.1 创建内容

1. 用户进入首页或“添加内容”页面。
2. 输入内容正文。
3. 可选填写标题。
4. 可选填写“我为什么保存”。
5. 可选填写来源链接。
6. 点击保存。
7. 系统立即创建内容记录。
8. 页面进入内容详情或处理状态页。
9. 后台开始生成摘要、标签和候选卡片。

### 7.2 AI 处理

AI 处理成功后，内容详情页显示：

- 内容标题；
- 内容摘要；
- 内容类型；
- AI 标签；
- 候选卡片；
- 处理状态；
- 原始输入；
- 来源链接。

AI 处理失败时：

- 保留用户输入；
- 显示“暂时无法生成卡片”；
- 提供重新生成按钮；
- 允许用户手动创建卡片。

### 7.3 确认卡片

用户在内容详情页查看候选卡片，可以：

- 确认；
- 编辑；
- 删除；
- 重新生成；
- 暂不处理。

确认后的卡片进入“我的卡片”，并可以参与回顾。

### 7.4 开始回顾

1. 用户进入“回顾”页面。
2. 系统展示一张待回顾卡片。
3. 用户先查看问题并尝试回忆。
4. 用户点击查看答案。
5. 用户选择记忆反馈：
   - 忘了；
   - 模糊；
   - 记得。
6. 系统记录回顾结果。
7. 用户进入下一张卡片或结束回顾。

MVP 可以暂时不根据反馈动态计算复杂复习间隔，只保存反馈记录并按简单规则推荐。

---

## 8. 页面需求

## 8.1 首页

首页重点是“继续输入”和“继续回顾”，不再展示多平台收藏推荐。

包含：

- 页面标题；
- 添加内容按钮；
- 今日待回顾数量；
- 最近创建的内容；
- 最近确认的卡片；
- 继续回顾按钮；
- 空状态提示。

首页不应做成营销页，用户进入后应直接看到可操作内容。

### 空状态

没有内容时显示：

- 简短说明；
- “添加第一条内容”按钮；
- 示例输入入口。

示例输入可以帮助用户理解格式，但不应自动写入用户数据。

## 8.2 添加内容页

字段：

- 标题，可选；
- 正文，必填；
- 我为什么保存，可选；
- 来源链接，可选；
- 内容类型，可选。

内容类型可以由用户选择，也可以默认由 AI 判断。

建议选项：

- 概念；
- 方法；
- 事实；
- 语言；
- 工作经验；
- 课程笔记；
- 其他。

交互要求：

- 正文为空时不能提交；
- 正文过短时允许保存，但提示可能无法生成卡片；
- 来源链接格式错误时提示，但不影响无链接内容保存；
- 保存按钮提交后必须有明确反馈；
- 提交过程中避免重复创建。

## 8.3 内容列表页

列表项显示：

- 标题；
- 摘要首行；
- 内容类型；
- 标签；
- 创建时间；
- 卡片状态；
- AI 处理状态。

支持：

- 关键词搜索；
- 按内容类型筛选；
- 按处理状态筛选；
- 按创建时间排序；
- 打开详情；
- 删除内容。

## 8.4 内容详情页

内容详情页是核心页面，包含：

1. 标题和编辑入口；
2. 内容类型；
3. 原文；
4. 用户保存原因；
5. 来源链接；
6. AI 摘要；
7. 标签；
8. 候选卡片；
9. 已确认卡片；
10. 重新生成入口；
11. 手动新增卡片入口；
12. 删除内容入口。

### 卡片候选区域

每张候选卡片应显示：

- 卡片类型；
- 问题或提示；
- 答案预览；
- 来源内容位置或来源片段；
- 确认按钮；
- 编辑按钮；
- 删除按钮。

## 8.5 卡片列表页

显示：

- 全部已确认卡片；
- 待回顾卡片；
- 最近创建卡片；
- 按内容类型筛选；
- 按标签筛选；
- 搜索卡片。

每个卡片列表项显示：

- 问题；
- 卡片类型；
- 所属内容；
- 上次回顾时间；
- 下次推荐时间；
- 当前状态。

## 8.6 回顾页

回顾页采用单卡片专注布局。

显示顺序：

1. 卡片问题；
2. “显示答案”按钮；
3. 答案内容；
4. 来源内容入口；
5. 回顾反馈按钮；
6. 下一张按钮。

在答案未显示前，不显示答案正文。

回顾完成后显示：

- 本次回顾卡片数；
- 新回顾卡片数；
- 用户可以继续回顾或返回首页。

## 8.7 设置页

包含：

- 账号信息；
- 隐私说明；
- 数据导出；
- 删除全部内容；
- 注销账号；
- 回顾设置；
- AI 生成设置；
- 退出登录。

---

## 9. AI 功能需求

### 9.1 AI 输入

AI 任务只允许使用：

- 用户输入的正文；
- 用户填写的标题；
- 用户填写的保存原因；
- 用户选择的内容类型。

来源链接在 MVP 中只作为展示字段，不作为 AI 解析依据。

### 9.2 AI 输出

建议使用结构化 JSON 输出：

```json
{
  "title": "费曼学习法",
  "summary": "通过主动解释发现知识漏洞，再针对漏洞重新学习并简化表达。",
  "content_type": "method",
  "tags": ["学习方法", "主动学习"],
  "cards": [
    {
      "type": "qa",
      "question": "费曼学习法的基本步骤是什么？",
      "answer": "用自己的话解释，找出不清楚的地方，重新学习，最后用更简单的方式表达。",
      "source_excerpt": "先尝试用自己的话解释一个概念..."
    }
  ]
}
```

### 9.3 服务端校验

服务端必须校验：

- JSON 格式；
- 字段类型；
- 卡片数量；
- 文本长度；
- 卡片问题不能为空；
- 卡片答案不能为空；
- `source_excerpt` 必须来自用户原文或为空；
- AI 输出不能覆盖用户原始正文；
- AI 任务失败后保留原始内容。

### 9.4 卡片生成规则

默认每条内容生成 1 至 3 张候选卡片。

以下情况可以生成 0 张卡片：

- 内容过短；
- 内容只是标题；
- 内容没有明确知识点；
- 内容是个人随笔但没有可回顾信息；
- AI 无法识别稳定的问答关系；
- 输出需要依赖来源链接中的未知内容。

AI 不应为了满足数量要求而强行生成卡片。

### 9.5 手动创建卡片

即使 AI 失败，用户也应能手动创建卡片。

手动卡片至少包含：

- 问题；
- 答案；
- 可选来源片段；
- 可选标签。

---

## 10. 卡片类型

### 10.1 问答卡

适用于概念、事实和理解型内容。

字段：

- 问题；
- 答案；
- 来源内容。

### 10.2 要点卡

适用于方法、清单和工作经验。

字段：

- 标题；
- 要点列表；
- 可选应用场景。

### 10.3 填空卡

适用于定义、结论和关键句。

字段：

- 原句；
- 遮挡内容；
- 正确答案。

MVP 可以先只实现问答卡，其他类型保留数据模型和产品入口，不必同时开发。

---

## 11. 卡片状态

统一使用以下状态：

- `candidate`：AI 已生成，等待用户处理；
- `confirmed`：用户已确认；
- `paused`：用户暂停回顾；
- `archived`：用户归档；
- `deleted`：用户删除。

卡片只有处于 `confirmed` 状态时，才可以进入回顾。

---

## 12. 回顾规则

### 12.1 MVP 推荐规则

第一版不要求实现完整的间隔重复算法。

可以采用以下简单规则：

- 新确认卡片：24 小时后推荐；
- 用户反馈“忘了”：次日推荐；
- 用户反馈“模糊”：3 天后推荐；
- 用户反馈“记得”：7 天后推荐；
- 用户连续多次“记得”：降低推荐频率；
- 用户跳过：不更新复习时间。

具体间隔可以配置在服务端，避免写死在前端。

### 12.2 回顾限制

- 单次默认推荐 3 至 5 张；
- 没有待回顾卡片时显示空状态；
- 用户可以随时结束；
- 不要求完成当天全部卡片；
- 不显示负向激励；
- 回顾过程中可以查看来源内容。

---

## 13. 数据模型建议

如果现有项目已经有 `Collection` 或收藏实体，应优先复用或平滑改造，不要在没有必要时同时维护两套相似实体。

### 13.1 Note / Content

```text
id
user_id
title
body
user_reason
source_url
content_type
ai_summary
ai_tags
processing_status
created_at
updated_at
last_opened_at
deleted_at
```

### 13.2 KnowledgeCard

```text
id
user_id
content_id
card_type
question
answer
source_excerpt
status
next_review_at
last_reviewed_at
created_at
updated_at
```

### 13.3 ReviewRecord

```text
id
user_id
card_id
feedback
reviewed_at
previous_interval
next_interval
```

### 13.4 ProcessingTask

```text
id
user_id
content_id
task_type
status
attempt_count
error_message
created_at
updated_at
```

### 13.5 Tag

```text
id
user_id
name
source
created_at
```

`source` 取值：

- `user`
- `ai`

### 13.6 用户隔离

所有查询和写入必须绑定当前用户：

```text
user_id = current_user.id
```

不得只通过前端传入的资源 ID 判断权限。

---

## 14. 后端接口建议

具体路径应遵循现有项目的 API 风格。以下为逻辑接口，不要求机械照搬路径。

### 内容

```text
POST   /contents
GET    /contents
GET    /contents/:id
PATCH  /contents/:id
DELETE /contents/:id
POST   /contents/:id/regenerate
```

### 卡片

```text
GET    /cards
POST   /cards
GET    /cards/:id
PATCH  /cards/:id
DELETE /cards/:id
POST   /cards/:id/confirm
POST   /cards/:id/pause
POST   /cards/:id/archive
```

### 回顾

```text
GET    /reviews/today
POST   /reviews/:card_id
```

### 搜索

```text
GET    /search?q=...
```

MVP 的搜索可以先使用数据库全文搜索或关键词匹配，不要求第一版接入向量数据库。

---

## 15. 异步处理要求

内容创建接口必须与 AI 处理解耦：

```text
创建内容
  ↓
立即返回 content_id
  ↓
创建 processing_task
  ↓
后台执行 AI 处理
  ↓
更新内容和候选卡片
```

要求：

- AI 处理失败可以重试；
- 任务应记录失败原因；
- 同一内容不能并发生成多个重复任务；
- 用户可以手动点击重新生成；
- 重新生成不应删除原始正文；
- 原有已确认卡片不能被静默覆盖；
- AI 生成结果写入前进行服务端校验。

---

## 16. 错误、空状态和边界情况

必须覆盖以下状态：

### 内容为空

提示用户输入正文后再保存。

### 内容过短

允许保存，但提示：

> 内容较短，可能无法生成有效卡片。

### AI 处理失败

显示：

- 原文仍已保存；
- 暂时无法生成卡片；
- 重新生成按钮；
- 手动创建卡片按钮。

### 没有候选卡片

显示：

> 这段内容暂时不适合生成卡片，你可以直接保存为知识笔记，或手动创建卡片。

### 没有已确认卡片

回顾页显示：

> 还没有可回顾的卡片。

并提供：

- 添加内容；
- 查看候选卡片；
- 手动创建卡片。

### 内容被删除

删除内容时：

- 相关候选卡片一起删除；
- 已确认卡片默认一并删除，或弹出明确选择；
- 回顾记录一并处理；
- 处理任务取消或标记为无效；
- 不允许出现无法访问来源的孤立卡片。

### 重复提交

用户连续点击保存时，只创建一条内容记录。

---

## 17. 隐私与安全要求

- 所有内容默认私密；
- 用户只能访问自己的内容、卡片和回顾记录；
- API 必须进行服务端权限校验；
- 不在日志中记录完整的敏感正文；
- AI 请求只发送完成任务所需的内容；
- 支持删除单条内容；
- 支持删除全部内容；
- 支持注销账号；
- 支持导出正文、卡片和回顾记录；
- 来源链接不会自动公开；
- 产品明确提示 AI 结果可能存在错误；
- 医疗、法律和金融内容不构成专业意见。

---

## 18. 视觉和交互要求

产品整体应体现“个人工具”和“轻量学习”的感觉：

- 页面简洁、安静、专注；
- 首页直接进入输入、内容或回顾；
- 不做营销型首页；
- 不使用复杂的仪表盘和装饰性模块；
- 卡片边界清晰，但避免大量嵌套卡片；
- 主要操作使用明确的文字和图标按钮；
- 回顾页面减少干扰；
- 问题和答案必须有明显的视觉区分；
- 移动端优先；
- 长文本需要自然换行；
- 所有按钮、输入框和列表项在小屏幕上不能溢出；
- 处理中的内容要显示明确状态；
- AI 结果应与用户原文区分展示。

---

## 19. Codex 实施顺序

### 第一步：审查现有代码

Codex 开始修改前，应先确认：

- 当前技术栈；
- 页面路由；
- 登录实现；
- 数据库模型；
- 现有收藏实体；
- AI 服务封装；
- API 组织方式；
- 测试框架；
- 当前未提交改动。

不得在未了解现有结构的情况下重建项目。

### 第二步：收窄现有数据流程

- 保留现有收藏或内容实体；
- 将核心输入改为正文；
- 来源链接改为可选字段；
- 移除核心流程对爬虫成功的依赖；
- 保留解析状态时，允许增加 `manual_input` 或 `user_provided` 状态；
- 确保内容保存接口可以独立工作。

### 第三步：实现内容创建

完成：

- 输入页；
- 内容保存接口；
- 内容列表；
- 内容详情；
- 编辑和删除；
- 用户数据隔离；
- 空状态和错误状态。

### 第四步：实现 AI 卡片生成

完成：

- 异步任务；
- 结构化 AI 输出；
- 服务端校验；
- 候选卡片展示；
- 卡片编辑；
- 确认、删除和重新生成。

### 第五步：实现回顾

完成：

- 待回顾卡片查询；
- 单卡片回顾界面；
- 显示答案；
- 回顾反馈；
- 下一次回顾时间；
- 回顾记录。

### 第六步：补齐验证

至少覆盖：

- 内容创建；
- 空内容校验；
- AI 失败后内容仍然存在；
- 用户不能访问其他用户内容；
- 候选卡片确认；
- 卡片删除；
- 回顾反馈；
- 内容删除后的关联数据处理。

---

## 20. 验收标准

### 内容输入

1. 用户可以输入标题和正文。
2. 用户可以不填写来源链接。
3. 用户可以不填写保存原因。
4. 正文保存成功后立即生成内容记录。
5. AI 处理失败不会导致正文丢失。
6. 重复点击保存不会创建重复记录。

### AI 处理

1. AI 可以基于正文生成摘要。
2. AI 可以生成 0 至 3 张候选卡片。
3. AI 无法判断时允许不生成卡片。
4. AI 输出经过服务端结构校验。
5. 卡片来源片段不能脱离用户原文。
6. AI 失败时用户可以重新生成或手动创建。

### 卡片管理

1. 用户可以确认候选卡片。
2. 用户可以编辑问题和答案。
3. 用户可以删除候选卡片。
4. 用户可以手动创建卡片。
5. 未确认的卡片不能出现在正式回顾中。
6. 用户可以暂停或归档已确认卡片。

### 回顾

1. 用户可以查看今日推荐卡片。
2. 答案默认隐藏。
3. 用户点击后才能查看答案。
4. 用户可以提交“忘了”“模糊”“记得”反馈。
5. 系统会保存回顾记录。
6. 系统会根据反馈计算下一次推荐时间。
7. 没有卡片时显示合理空状态。
8. 用户可以中途结束回顾。

### 权限和数据

1. 用户只能查看自己的内容。
2. 用户只能修改和删除自己的内容。
3. 删除内容后，不产生无法访问的孤立卡片。
4. 用户可以删除全部个人数据。
5. 用户可以导出核心数据。
6. 默认不公开任何内容。

### 移动端体验

1. 手机窄屏下内容不横向溢出。
2. 长文本能够正常换行。
3. 保存、生成、确认和回顾都有加载状态。
4. 网络错误时提供重试入口。
5. 关键按钮不会被底部导航遮挡。
6. 回顾页面在常见 iPhone 屏幕尺寸下可正常操作。

---

## 21. 第一版完成定义

当以下流程能够稳定运行时，MVP 即达到完成标准：

```text
用户输入一段文字
  ↓
内容被保存
  ↓
AI 生成摘要和候选卡片
  ↓
用户确认或编辑卡片
  ↓
卡片出现在卡片列表
  ↓
用户完成一次回顾
  ↓
系统记录反馈并安排下一次回顾
```

第一版不以“支持多少平台”为完成标准，而以以下结果为完成标准：

> **用户能够可靠地输入一段内容，把它变成自己认可的卡片，并在之后重新想起它。**

---

## 22. 给 Codex 的最终执行指令

请基于当前项目已有的技术栈、页面结构、数据模型和组件体系完成以下工作：

1. 先审查现有项目，不重建已有基础设施。
2. 将产品核心从外部链接自动解析调整为手动文本输入。
3. 保留来源链接字段，但不让链接解析阻塞内容创建。
4. 优先完成 P0，不主动扩展 P1 和 P2。
5. 保证原文永远优先保存，AI 处理采用异步流程。
6. 实现 AI 摘要和候选卡片生成。
7. 实现候选卡片的确认、编辑、删除和手动创建。
8. 实现基础卡片回顾和反馈记录。
9. 保证所有用户数据在服务端按用户隔离。
10. 为加载、空、失败、重试、无卡片和权限错误补齐界面状态。
11. 使用现有项目的命名、样式、路由和 API 约定。
12. 不实现视频、图片、爬虫和平台链接自动解析，除非后续明确提出。
13. 完成实现后运行现有测试、类型检查和构建流程。
14. 最后说明修改了哪些文件、完成了哪些功能、哪些功能仍未实现，以及验证结果。

最终产品应当围绕这个最小、可靠的核心体验：

> **输入值得记住的内容，生成可信的卡片，之后真的记住它。**