# 「先存着」指标与验收口径 v0.2

> 目标值均为验证期建议值，上线后按真实基线校准；所有指标先按"登录用户 + 测试账号"统一统计。

## 1. 北极星指标

**每周被再次使用的收藏数量（人均）**

- 定义：自然周内，被"再次使用"过的收藏，按用户去重后的人均值。
- 再次使用 = 重新打开收藏详情 / 搜索结果命中点击 / 加入收藏集 / 确认知识卡片 / 通过发现页打开，任一即计。
- 分子：本周被再次使用的收藏数（同一收藏多次使用只计一次）。
- 分母：本周活跃用户数（有任意行为事件的用户）。
- 目标（验证期第 4 周）：人均 ≥ 3 条；同时监控"周收藏再次使用率 = 被再次使用的收藏数 ÷ 活跃用户拥有的收藏总数"≥ 20%。
- 数据来源：详情打开、搜索点击、收藏集关联、卡片确认、发现页打开五类埋点事件。

## 2. 收藏夹主指标

| 指标 | 口径 | 建议目标 |
|---|---|---|
| 首次收录成功率 | 提交后成功保存的收录 ÷ 提交收录总次数 | ≥ 95% |
| 7 日再次收录率 | 首次收录的用户中，7 日内再次收录 ≥ 1 次的用户 ÷ 首次收录用户 | ≥ 40% |
| 周人均有效收录数 | 周成功保存的收录 ÷ 周活跃用户 | ≥ 3 条 |
| 搜索结果点击率 | 点击任一结果的搜索会话 ÷ 搜索会话总数 | ≥ 30% |
| 搜索成功率 | 产生点击或有效 AI 回答的搜索会话 ÷ 搜索会话总数 | ≥ 70% |
| 收藏再次打开率 | 30 天内打开过详情 ≥ 1 次的收藏 ÷ 期间可查看收藏 | ≥ 30% |
| 重新发现点击率 | 发现页推荐位点击 ÷ 推荐位展示 | ≥ 10% |
| AI 分类标签修改率 | 被修改的 AI 生成标签 ÷ AI 生成标签总数 | ≤ 30%（越低越好） |
| 完整解析率 | 状态为"完整解析"的收藏 ÷ 已进入解析的收藏 | ≥ 70% |
| 解析失败率 | 状态为"解析失败"的收藏 ÷ 已进入解析的收藏 | ≤ 5% |

## 3. 知识功能专项指标

| 指标 | 口径 | 建议目标 |
|---|---|---|
| 知识内容识别率 | 抽样人工确认正确的识别结果 ÷ 知识类收藏（抽样） | ≥ 80% |
| 候选卡片生成成功率 | 生成 ≥ 1 张候选卡片的知识收藏 ÷ 适合生成的知识收藏 | ≥ 60% |
| 候选卡片确认率 | 被用户确认的候选卡片 ÷ 候选卡片总数 | ≥ 40% |
| 卡片编辑删除比例 | 被编辑或删除的卡片 ÷ 已生成卡片 | ≤ 30%（越低越好） |
| 卡片首次回顾率 | 确认后 7 日内回顾 ≥ 1 次的卡片 ÷ 已确认卡片 | ≥ 50% |
| 7 日内再次回顾率 | 确认后 7 日内回顾 ≥ 2 次的卡片 ÷ 已确认卡片 | ≥ 25% |

## 4. MVP 验收标准（量化版）

1. 通过快捷指令或粘贴链接收录成功率 ≥ 95%。
2. 解析失败时原始链接保存兜底率 = 100%。
3. P0 功能全部可用：查看、搜索、筛选、备注、收藏集、批量操作无阻塞性缺陷（P0/P1 缺陷为 0）。
4. 所有 AI 结果 100% 可追溯到来源收藏，摘要/标签/分类均可修改并回写。
5. 视频缺少可靠文本时，虚构摘要与卡片生成率 = 0%，一律标记"部分解析"或"仅保存链接"。
6. 关键词与自然语言搜索成功率 ≥ 70%（用户获得有效结果或答案）。
7. 发现页上线 ≥ 3 类推荐位，每条推荐必须有依据字段（上次打开时间 / 主题相关度等）；发现页点击率 ≥ 10%。
8. 知识类收藏中 ≥ 60% 生成 1–3 张候选卡片。
9. 进入回顾或记忆计划的卡片 100% 经过用户确认。
10. 默认私密：未主动操作时所有数据仅本人可见，上线前完成越权访问安全测试（跨用户读取为 0）。

## 5. 埋点与数据来源

- 行为埋点：收录提交、解析完成、详情打开、搜索发起与点击、筛选、发现页展示与点击、卡片生成/确认/编辑/删除、回顾开始与反馈。
- 数据库：Collection、KnowledgeCard、ReviewRecord、ProcessingTask 的状态与时间字段。
- 抽样质检：每周抽取 50 条 AI 解析结果和 30 张卡片，人工核对准确率与幻觉率。

## 6. 口径说明

- 活跃用户：当日或当周产生任意行为事件的登录用户。
- 收藏总数：统计周期内用户可见的收藏（不含已删除）。
- 解析相关指标分母不含"待解析"。
- 所有"周"指标按自然周（周一至周日）统计。
